# ZapRemote

iOS TV remote for LG TVs — supports NetCast ROAP (HTTP :8080) and webOS (WebSocket :3000) with automatic discovery.

## Requirements

- Xcode 16+
- iPhone on the same Wi‑Fi network as the TV

## Open

Open `ZapRemote/ZapRemote.xcodeproj` in Xcode, select your iPhone, and run.
